{
  "name": "jules-discord-bridge",
  "version": "0.1.0",
  "description": "Persistent per-channel Jules SDK bridge for Discord — file-based access control.",
  "type": "module",
  "main": "bridge.mjs",
  "scripts": {
    "start": "node bridge.mjs",
    "test": "npm run test:unit",
    "test:unit": "node --test test/unit/*.test.mjs",
    "test:smoke": "node test/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@google/jules-sdk": "^0.2.0",
    "discord.js": "^14.26.3",
    "dotenv": "^17.4.2"
  }
}
